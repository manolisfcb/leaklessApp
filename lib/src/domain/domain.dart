/// Barrel for the whole domain layer (models + enums).
///
/// The domain is pure Dart: it must not import Flutter, Supabase, Firebase or
/// RevenueCat (quality rule #7).
library;

export 'enums/enums.dart';
export 'models/models.dart';
